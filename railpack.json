{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["npm install --include=dev"]
    }
  },
  "deploy": {
    "startCommand": "npx ts-node -P tsconfig.agent.json agent/index.ts"
  }
}
